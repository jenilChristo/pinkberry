<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BabyChloe.Mobile.Views.Activity.ActivityFeedPage"
             Title="Activity">

    <RefreshView Command="{Binding RefreshCommand}" IsRefreshing="{Binding IsRefreshing}">
        <CollectionView ItemsSource="{Binding Activities}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="12" Margin="8,4" CornerRadius="8">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                            <Label Text="{Binding Type}" FontSize="24" VerticalOptions="Center" />
                            <VerticalStackLayout Grid.Column="1">
                                <Label Text="{Binding Description}" FontSize="15" />
                                <Label Text="{Binding Timestamp, StringFormat='{0:HH:mm}'}" FontSize="12" TextColor="Gray" />
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <Label Text="No activities yet today. Start tracking! 🌟"
                       HorizontalOptions="Center" VerticalOptions="Center" Margin="0,100,0,0" />
            </CollectionView.EmptyView>
        </CollectionView>
    </RefreshView>
</ContentPage>